NODE_ENV=development
PORT=3000
LOG_LEVEL=info
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/__package__?schema=public

# CORS — comma-separated origins (empty disables CORS)
CORS_ORIGINS=http://localhost:5173

# Swagger UI exposed at /api/docs (set to 'false' in production)
SWAGGER_ENABLED=true
