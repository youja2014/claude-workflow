# Design — N/A

본 메타 프로젝트는 **UI 가 없습니다**. CLI / hook / skill / agent / rule 의 텍스트 산출물만 다룹니다.

`design/` 폴더는 docs/ 7 카테고리 일관성을 위해 보존하지만 실 컨텐츠는 없습니다.

UI 가 있는 산출물 (예: scaffold 가 만들어내는 `templates/ts-nx/apps/web/`) 의 디자인 문서는 **그 프로젝트의 자체 `docs/design/`** 에 들어가야 합니다.
